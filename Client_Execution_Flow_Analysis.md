# 🎮 게임 클라이언트 실행 및 진행 구조 분석

본 문서는 현재 프로젝트의 클라이언트에서 게임이 어떻게 초기화되고, 매 프레임 어떤 순서로 동작하며, 화면에 렌더링되는지에 대한 전체적인 구조를 분석한 문서입니다.

## 1. 개요 및 핵심 아키텍처 (Architecture Overview)

이 프로젝트는 **싱글 스레드 기반의 로컬 클라이언트 게임 루프**를 메인으로 하되, 리소스 로딩 등 일부 작업에만 멀티 스레드를 활용하는 구조를 가집니다. 네트워크 동기화 방식이 아닌 **독립적인 로컬 실행 형태**이며, 다음과 같은 특징을 갖습니다.

*   **컴포넌트 기반 아키텍처 (Component-based Architecture):** 오브젝트의 동작과 시각적 표현은 렌더 대상 등록 및 컴포넌트의 Update/렌더를 통해 이루어집니다.
*   **월드 기반 상태 관리 (World-based State Management):** Start, Loading, Main, Editor 등 게임의 각 씬(Scene) 상태를 독립적인 `World` 객체로 관리하고 단계적으로 전환합니다.
*   **매 프레임 단방향 파이프라인:** `입력(Input) 처리` ➡️ `월드 및 오브젝트 논리(Update)` ➡️ `충돌 판정(Collision)` ➡️ `렌더링(Rendering)`의 명확하고 단방향적인 사이클을 가집니다.

---

## 2. 엔진 초기화 및 시작 과정 (Initialization)

프로그램이 시작되면 엔진 구동을 위한 전반적인 서브시스템 초기화와 첫 월드 전환이 발생합니다.

*   **시작 지점:** `Client/main.cpp:27`
    *   `CEngine::Init()`을 호출하여 엔진을 초기화합니다.
    *   클래스 기본 객체(CDO, Class Default Object)를 엔진에 등록합니다.
    *   시작 월드(`CStartWorld`)를 생성하고 세팅합니다.
*   **시스템 초기화 순서:** `GameEngine/Engine.cpp:51`
    *   `Device` (DirectX 및 그래픽 디바이스 초기화)
    *   `Asset` (리소스 및 에셋 매니저 초기화)
    *   `Render` (렌더 매니저 및 렌더 타겟/파이프라인 초기화)
    *   `Thread` (스레드 풀 및 로딩 스레드 초기화)
    *   `WorldManager` (월드 전환 및 관리 구조)
    *   `Timer` (시간 계산)
*   **메인 루프 진입:** `Client/main.cpp:49, 51`에서 초기화를 마치고 Windows 메시지 루프와 함께 엔진 메인 루프에 진입합니다.

---

## 3. 메인 게임 루프 (Main Frame Loop)

게임 진행의 핵심 뼈대이며, 매 프레임마다 엔진 로직을 실행합니다.

*   **루프 본체:** `GameEngine/Engine.cpp:102 (Run)`
    *   운영체제 메시지가 없을 경우 `Logic()` 함수를 반복 실행합니다 (`:141`).
*   **Logic() 내부 흐름:**
    1.  **DeltaTime 계산:** 이전 프레임과 현재 프레임 사이의 시간차를 계산합니다.
    2.  **월드 처리:** `WorldManager::Update()`, `PostUpdate()` 순으로 현재 활성화된 월드의 로직을 처리합니다 (`:155`).
    3.  **렌더링 처리:** `RenderManager::Update()` 및 실제 렌더링 과정을 호출합니다 (`:166`).

---

## 4. 월드 내부 로직 진행 순서 (World Update & Pipeline)

프레임 업데이트 시 해당 월드에 속한 객체들이 규칙에 따라 업데이트됩니다. (`GameEngine/World/World.cpp:70`)

### 4.1. Update 단계 (`:75 ~ :112`)
1.  **입력 (Input) 업데이트:** 사용자 키보드/마우스 입력을 가장 먼저 캡처합니다.
2.  **오브젝트 (Object) 업데이트:** 월드 내 존재하는 게임 오브젝트(`GameObject`)들의 로직을 실행시킵니다.
3.  **기능성 객체 업데이트:** `Camera`, `Asset`, `Navigation`(길찾기), `UI` 매니저 등의 Update를 차례로 수행합니다.

### 4.2. PostUpdate 및 충돌 단계 (`:115 ~ :149`)
1.  **오브젝트 PostUpdate:** 기본 로직 이후에 처리해야 할 위치/상태 후처리를 수행합니다.
2.  **충돌 처리 (Collision):** 모든 오브젝트의 갱신이 끝난 후, 겹침이나 물리적 충돌 이벤트를 판단하고 처리합니다.
3.  **Transform 갱신:** 부모 객체 이동 및 충돌 보정 등을 반영하여 객체의 최종 월드 변환 행렬(Transform Matrix)을 갱신합니다.

> 💡 **심화 분석: 입력, UI, 충돌의 우선순위**
> *   **입력 처리:** `GameEngine/World/Input.cpp:59 (CInput::Update)`에서 시스템 키 상태/마우스 입력 및 사용자가 명시한 바인딩 함수가 일괄 처리됩니다 (`:639`).
> *   **충돌 우선순위 (UI vs World):** 충돌 단계에서는 화면 최상단의 UI부터 판정합니다. `GameEngine/World/WorldCollision.cpp:94`를 보면, **UI 충돌 여부를 최우선으로 연산**하고 여기서 마우스가 UI 위에 없을 경우에만(미충돌 시), **월드 내 3D/2D 오브젝트의 마우스 레이캐스팅/충돌(`:100`)을 판정**합니다. 이는 마우스로 UI 버튼을 클릭할 때 뒷배경의 유닛이나 지형이 동시에 클릭되는 현상을 막아주는 필수적인 방어 로직입니다.

---

## 5. 렌더링 파이프라인 (Render Pipeline)

렌더링은 논리적으로 변화된 객체들의 데이터를 모아 실제 화면의 픽셀로 변환하는 작업입니다. `GameEngine/Render/RenderManager.cpp:479`에서 그 전체 과정이 지휘됩니다.

### 5.1. 렌더 등록 (Render Registration)
*   게임 오브젝트가 화면에 그려지려면 자신의 컴포넌트 초기화(`Begin`) 순간에 렌더 매니저의 특정 렌더 레이어에 등록(Register)되어야 합니다. (`GameEngine/Component/SceneComponent.cpp:101, 117`)

### 5.2. 프레임 드로우 순서 (Drawing Order)
전체적인 렌더링 과정은 다음과 같은 흐름을 따릅니다.
1.  **월드 렌더링:** `MainTarget` 이라는 중간 렌더 타겟(Render Target) 버퍼에 맵, 데칼, 기본 게임 오브젝트(캐릭터, 배경 등), 투명/반투명 객체를 정해진 레이어 순서대로 그립니다.
2.  **포스트 프로세싱 (Post-process):** 화면 전체 렌더 결과를 바탕으로 후처리 시각 효과(Bloom, 블러, 색 보정 등)를 적용합니다.
3.  **최종 백버퍼 출력:** 중간 연산이 완료된 `MainTarget`의 결과물을 실제 모니터로 송출될 백버퍼(`BackBuffer`)로 복사/출력합니다 (`:618`).
4.  **UI 렌더링:** 3D 게임 월드 위에 깊이 판정 없이 덮어씌워져야 하므로 백버퍼에 직접 2D UI 항목들을 그립니다 (`:623`).
5.  **마우스 렌더링:** 시스템 OS 마우스 커서를 감추고 자체 구현한 마우스 텍스처 위젯 등을 제일 마지막에 렌더링합니다.

---

## 6. 생명주기 및 씬(월드) 전환 처리 (Flow Control)

시작 화면 ➡️ 로딩 ➡️ 메인 게임 ➡️ 에디터 등으로 이어지는 게임 플로우 제어 구조입니다.

1.  **전환 요청:**
    시작 화면의 UI 버튼(Start 버튼 등) 클릭 시, 엔진은 `Client/UI/StartWidget.cpp:136 (StartClick)` 로직을 통해 차기 월드를 `LoadingWorld`로 전환하고, 최종 로딩할 목적지 타입(예: MainWorld)을 지정합니다.
2.  **비동기 로딩 스레드:**
    로딩 화면 전용 월드인 `LoadingWorld`로 상태가 진입하면, 메인 스레드는 로딩 애니메이션 기믹이나 프로그레스바 UI 처리를 계속 렌더링합니다. 동시에, 백그라운드 스레드를 별도로 생성하여 무거운 다음 월드 에셋 파싱/메모리 적재 등을 **비동기적으로 수행**합니다. (`Client/World/LoadingWorld.cpp:46`) <br>
    *(실제 백그라운드 스레드 작업 지점: `Client/Thread/LoadingThread.cpp:20`의 `CreateAsyncWorld<>`)*
3.  **스왑(Swap) 및 완료:**
    비동기 월드 생성이 전부 완료되면 `CompleteAsyncWorld()`로 완료 플래그를 세우고(`Client/World/LoadingWorld.cpp:37`), 다음 프레임에 `WorldManager`가 이를 인지하여 지연 없이 실제 활성 월드를 타겟 월드로 스왑(Swap)하여 게임 플레이로 매끄럽게 넘어갑니다. (`GameEngine/World/WorldManager.cpp:71`)

---

## 7. 핵심 요약 (Summary)
**"해당 엔진은 철저한 클라이언트-단일 루프 기반 아키텍처를 채택하여, 매 프레임마다 명확한 데이터 파이프라인(`입력` ➡️ `트리 업데이트/계산` ➡️ `UI/월드 충돌 처리` ➡️ `레이어 기반 렌더링`) 흐름을 유지합니다. 동시에 `WorldManager`를 거친 상태계 기계(FSM) 방식으로 씬을 제어하고, 멀티 스레드를 통한 백그라운드 레벨 로딩을 지원하여 구조적 안정성과 부드러운 사용자 경험을 보장합니다."**
